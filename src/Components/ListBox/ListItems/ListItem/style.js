import styled from 'styled-components';

export const Wrapper = styled.div`
  display: flex;
  justify-content: space-around;
  font-size: 13px;
  
  p:first-child{
    color: #55ccc1;
  }
  p:last-child{
    color: #555;
    white-space: nowrap;
  }
`;
